// Canonical public URL for hotel-facing RFP links (used in invitation emails,
// reminders, and the "Copy link" button). Hard-set to our trusted domain so a
// hotel's link never depends on which domain a staff member happens to be
// viewing the dashboard from.
export const PUBLIC_APP_URL = 'https://rfp.kjsportstravel.com'
